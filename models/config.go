//Package models (config.go): all the information needed to configure the connection to the database is stored here
package models

// Prod info for connectiong to school db
const (
	host   = "localhost"
	port   = "5432"
	dbname = "group_153"
	user   = "postgres"
	dbcode = "SGFsbGVsdWphaDEx"
)
